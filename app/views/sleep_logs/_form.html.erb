<%= form_for(@sleep_log) do |f| %>
  <% if @sleep_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sleep_log.errors.count, "error") %> prohibited this sleep_log from being saved:</h2>

      <ul>
      <% @sleep_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :sleep %><br>
    <%= f.datetime_select :sleep %>
  </div>
  <div class="field">
    <%= f.label :wake_up %><br>
    <%= f.datetime_select :wake_up %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
